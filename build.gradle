subprojects { subproject ->
    apply plugin: 'java'

    repositories {
        jcenter()
    }

    dependencies {
        compile 'com.fieldju:commons:1.2.0'
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.5'
}